"""The platform's version, in one place.

It is stamped into every run's metadata and reproduction script (jobs.py) and
it is what the citation says (citation.py), so those two can never disagree
about which version produced a result.
"""

PLATFORM_VERSION = "0.2.0"
